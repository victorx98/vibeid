ANTHROPIC_API_KEY=
ENTITLEMENTS_SECRET=
BILLING_KILL_SWITCH=false

# Supabase Auth + Postgres
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
# Temporary fallback while cutting over from legacy Supabase keys.
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=
SQLITE_KB_PATH=data/resume_material_library.db

# Stripe Checkout
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_BASIC=
STRIPE_PRICE_RESUME=
BILLING_ENABLED=false

# Demo-only unlock minting is off unless explicitly enabled.
NEXT_PUBLIC_DEMO_UNLOCKS_ENABLED=false

# The static Vibe ID sample embed is off unless explicitly enabled.
NEXT_PUBLIC_ENABLE_VIBE_SAMPLE=false
